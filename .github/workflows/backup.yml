name: Backup Website
on:
  schedule:
    - cron: '0 3 * * *'  # Daily at 3 AM UTC
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create Backup
        run: |
          echo "Backup created $(date)" >> backup-log.txt
      - name: Upload to DBOT Vault
        run: |
          # Could upload to a backup repo
          echo "Backup complete"
